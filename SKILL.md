---
name: manga-png-to-paperback
description: PNG連番画像フォルダをKindle KDPペーパーバック入稿用PDFに変換する。ユーザーが「PNGをPDFにして」「マンガのPNGをPDFに」「KDP用PDF作って」「入稿用PDF作って」「ペーパーバック用PDF作って」など、画像ページから紙本PDFを作りたいと言ったときに使う。Word原稿やEPUB作成は対象外。
---

# Manga PNG to Paperback PDF

PNG連番フォルダから、KDPペーパーバック用PDFを作る単品スキル。親チーム `kdp-conversion-team` からも呼ばれる。

## 軽量チーム

- 受付: PNGフォルダ、判型、裁ち落とし、ノンブル、余白、出力先を確認する
- 変換係: `scripts/manga_pdf_maker.py` を使ってPDF化する
- 検品係: ページ順、画像サイズ、判型、裁ち落とし、余白、ノンブル、全ページ同一サイズを確認する

PNG系なので、Kindle原稿クリーナー確認はしない。

## 確認すること

最初に必要最小限だけ聞く。

- PNGフォルダのパス
- 判型: `A5` / `B6`
- 裁ち落とし: あり / なし
- ノンブル: あり / なし
- 余白の追加: 余白なし画像なら `y`、余白あり画像なら `n`

ノンブルありの場合だけ追加で聞く。

- ノンブル開始ページ
- スキップするページ
- 強制的に白抜きにしたいページ

裁ち落としありの場合は、塗り足し3mmつきのテンプレートで作った画像か確認する。見た目だけで勝手に判断しない。

## 実行方法

スクリプトは対話式なので、そのまま実行すると止まりやすい。回答を集めてから `printf` で渡す。

ノンブルなし:

```bash
printf 'A5\nなし\nなし\ny\n' \
  | python3 ~/.codex/skills/manga-png-to-paperback/scripts/manga_pdf_maker.py "/path/to/png-folder"
```

ノンブルあり:

```bash
printf 'A5\nなし\nあり\n002\n001.024\n005.010\ny\n' \
  | python3 ~/.codex/skills/manga-png-to-paperback/scripts/manga_pdf_maker.py "/path/to/png-folder"
```

質問の順序。

1. 判型: `A5` または `B6`
2. 裁ち落とし: `あり` または `なし`
3. ノンブル: `あり` または `なし`
4. ノンブル開始ページ: ノンブルありの場合のみ
5. スキップするページ: ノンブルありの場合のみ。なければ空行
6. 強制白抜きページ: ノンブルありの場合のみ。なければ空行
7. 余白の追加: `y` または `n`

## 検品

作成後に最低限見る。

- PDFが作成されている
- 画像枚数とPDFページ数が合っている
- ファイル名の自然順で並んでいる
- 全ページ同一サイズ
- 判型が指定どおり
- 裁ち落とし指定が意図どおり
- ノンブルありの場合、開始ページ、スキップ、白抜き指定が反映されている

完了時は、出力PDFパス、ページ数、判型、裁ち落とし、ノンブルの有無を短く返す。
